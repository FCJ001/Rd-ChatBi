# ============================================================
# rd-chatBI 环境变量样例
# 用法：cp .env.example .env 然后填真实密钥
# .env 已在 .gitignore 里，绝不提交
# ============================================================

# 应用
APP_NAME=rd-chatBI
APP_ENV=dev
APP_DEBUG=true

# PostgreSQL — 复用 rd-agent-platform 的 PG 实例，本项目用独立的 rd_chatbi 库
DB_HOST=localhost
DB_PORT=5432
DB_USER=rdagent
DB_PASSWORD=rdagent123
DB_NAME=rd_chatbi

# NL2SQL 查询目标 — demo 医院运营库（scripts/init_demo_data.py 初始化）
DEMO_DB_USER=rdagent
DEMO_DB_PASSWORD=rdagent123
DEMO_DB_NAME=chatbi_demo

# Redis — 复用 rd-agent-platform 的 Redis（key 前缀隔离）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Milvus — 复用 rd-agent-platform 的 Milvus（collection 前缀 chatbi_* 隔离）
MILVUS_HOST=localhost
MILVUS_PORT=19530

# Elasticsearch — 列值召回（本项目 docker-compose 自带 chatbi-es）
ES_HOST=localhost
ES_PORT=9200

# ---------------- 模型 ----------------
DASHSCOPE_API_KEY=sk-xxxxxxxx
BASE_URL_CHAT=https://dashscope.aliyuncs.com/compatible-mode/v1
CHAT_MODEL=qwen-max
EMBEDDING_MODEL=text-embedding-v3

DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_MODEL=deepseek-chat

# 模型定价（USD/1M tokens，token 计费用）
MODEL_PRICING_INPUT=0.4
MODEL_PRICING_OUTPUT=1.2

# API 限流（按 user_id 滑动窗口）
# RATE_LIMIT_BACKEND=redis（ZSET 跨 worker）/ memory（进程内 deque）
RATE_LIMIT_ENABLED=true
RATE_LIMIT_BACKEND=redis
RATE_LIMIT_MAX_REQUESTS=20
RATE_LIMIT_WINDOW_SECONDS=60

# ---------------- 日志 ----------------
LOG_LEVEL=DEBUG
LOG_DIR=logs
