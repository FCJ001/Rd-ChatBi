# ============================================================
# rd-chatBI 依赖
#
# 与 rd-knowledge-svc / rd-agent-platform 保持版本一致，已验证跑通。
# ChatBI 专项服务：NL2SQL（9 阶段流水线 + 四层防线）+ 图表推荐渲染。
#
# 关键依赖：sqlglot（AST 层 SQL 注入防护）、jieba（关键词提取）、
#           elasticsearch（列值召回）、pymilvus（元数据向量召回）
# ============================================================

# ---------------- Web 框架 ----------------
fastapi==0.135.1
aiofiles==25.1.0
uvicorn==0.42.0
python-multipart==0.0.22
sse-starlette==3.3.4
httpx==0.28.1

# ---------------- 认证 ----------------
pyjwt==2.10.1

# ---------------- 配置与日志 ----------------
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2
loguru==0.7.3

# ---------------- PostgreSQL ----------------
SQLAlchemy==2.0.48
asyncpg==0.30.0
greenlet==3.3.2
psycopg2-binary==2.9.11

# ---------------- 测试 ----------------
pytest==9.0.2
pytest-asyncio==1.3.0

# ---------------- Milvus ----------------
pymilvus==2.6.10

# ---------------- Redis ----------------
redis==7.3.0

# ---------------- 可观测性 ----------------
prometheus-client==0.25.0

# ---------------- LangChain / LangGraph ----------------
langchain==1.2.15
langchain-core==1.2.28
langchain-community==0.4.1
langchain-openai==1.1.12
langgraph==1.1.6
dashscope==1.25.16

# ---------------- 数据处理 / 图表 ----------------
pandas==2.3.3
# plotly/kaleido 已移除：服务端图表渲染改由前端 ECharts 承担（echarts_builder）

# ---------------- NL2SQL（AST 层 SQL 注入）----------------
sqlglot==26.17.0

# ---------------- NL2SQL RAG 流水线 ----------------
jieba==0.42.1
elasticsearch==8.15.1
pyyaml==6.0.2
